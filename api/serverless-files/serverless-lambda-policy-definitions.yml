- Effect: Allow
  Action:
    - "s3:GetObject"
  Resource: arn:aws:s3:::${self:custom.environment.S3_KN_REPORTS_BUCKET}/*
- Effect: Allow
  Action:
    - "s3:GetObject"
    - "s3:PutObject"
    - "s3:DeleteObject"
  Resource: arn:aws:s3:::${self:custom.environment.S3_WEBSITE_BUCKET}/*